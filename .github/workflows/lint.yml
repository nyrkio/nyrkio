name: Lint

on: [pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build CI image
        run: docker build -t nyrkio-ci -f Dockerfile.ci .
      - name: Lint
        run: docker run --rm nyrkio-ci make lint

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build CI image
        run: docker build -t nyrkio-ci -f Dockerfile.ci .
      - name: Format check
        run: docker run --rm nyrkio-ci make format-check
